online at ddate.site

## Donations (`/fnord`)

Set optional env vars (see `.env.example`):

- `BTC_DONATION_ADDRESS`
- `ETH_DONATION_ADDRESS`

If neither is set, the fnord page still loads but omits the crypto block.
